import { UnIdenfiedDeadBody } from './un-idenfied-dead-body';

describe('UnIdenfiedDeadBody', () => {
  it('should create an instance', () => {
    expect(new UnIdenfiedDeadBody()).toBeTruthy();
  });
});
